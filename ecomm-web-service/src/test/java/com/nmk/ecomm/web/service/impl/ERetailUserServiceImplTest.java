package com.nmk.ecomm.web.service.impl;

/*@RunWith(MockitoJUnitRunner.class)
*/public class ERetailUserServiceImplTest {
/*	private static ERetailUserServiceImpl mockUser;
	private MockMvc mockmvc;
	*//**
	 * @BeforeClass .
	 *//*
	@BeforeClass
	public static void init() throws IllegalAccessException, InvocationTargetException {
		mockUser = mock(ERetailUserServiceImpl.class);
		String pwd = "testing";
		int uid =1 ;
		Boolean value = true;
		when(mockUser.getUserByLogin(uid, pwd)).thenReturn(value);
	}
	
	@Test
	public void getLoginTest() throws IllegalAccessException, InvocationTargetException {
		int uid = 1;
		String pwd = "testing";
		final boolean status = mockUser.getUserByLogin(uid, pwd);
		assertEquals(true, status);
	}
*/
}
